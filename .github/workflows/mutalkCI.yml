name: MutalkCI

on:
  pull_request_target:
    branches: [ main ]
    paths-ignore:
    - 'README.md'
    - 'resources/**'

jobs:
  uses: pharo-contributions/mutalk/.github/workflows/mutalk-workflow.yml
